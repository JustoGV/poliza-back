# Sin hoisting: si un paquete no está declarado en package.json, no resuelve.
# Evita dependencias fantasma que rompen en CI y no en local.
node-linker=isolated
strict-peer-dependencies=false
auto-install-peers=true
engine-strict=true
